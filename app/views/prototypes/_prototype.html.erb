<div class="card">
  <%= link_to image_tag(prototype.image, class: :card__img ), prototype_path(prototype) %>
  <div class="card__body">
    <%= link_to prototype.title, prototype_path(prototype), class: :card__title %>
    <p class="card__summary">
      <%= prototype.catch_copy %>
    </p>
    <%= link_to "by #{prototype.user.name}", user_path(prototype.user.id), class: :card__user %>
  </div>
</div>


<%# 以下の４つを変更する %>
<%# "logo.png"、"プロトタイプのタイトル"（終了）、 "プロトタイプのキャッチコピー"（終了）、"by プロトタイプの投稿者名" %>

<%# root_pathを変更する。クリックしたら投稿者のページに遷移する。 %>
<%# prototype_path(current_user) %>